import React from 'react';
import styles from './PieChart.module.css';
import { Pie, PieChart, ResponsiveContainer } from 'recharts';
import { type PieLabelRenderProps } from 'recharts';
import { type TTwoPieCharts } from '../types';

/**
 * Тип данных для круговой диаграммы после обработки
 * @typedef {Object} TPieChartData
 * @property {string} name - Название категории/показателя
 * @property {number} prevValue - Значение предыдущего периода (после валидации)
 * @property {number} currentValue - Значение текущего периода (после валидации)
 */

type TPieChartData = {
  name: string;
  prevValue: number;
  currentValue: number;
};

/**
 * Компонент парных круговых диаграмм для сравнения показателей инициативы
 *
 * @component TwoPieCharts
 * @description
 * Компонент отображает две одинаковые круговые диаграммы рядом для визуального сравнения
 * показателей инициативы между двумя временными периодами. Каждая категория представлена
 * сегментом диаграммы, а первая буква названия категории отображается на белом кружке
 * рядом с соответствующим сегментом.
 *
 * **Ключевые особенности:**
 * - Две идентичные по структуре круговые диаграммы для сравнения "до" и "после"
 * - Строгая валидация входных данных (диапазон 0-100%)
 * - Кастомные метки с первой буквой названия категории на белом фоне
 * - Одинаковый размер и положение диаграмм для честного сравнения
 * - Адаптивные контейнеры (ResponsiveContainer) для сохранения пропорций
 * - Отключенная анимация для стабильности отображения
 *
 * **Визуальное представление:**
 * - Левая диаграмма: значения предыдущего периода (класс .pie_prev)
 * - Правая диаграмма: значения текущего периода (класс .pie_current)
 * - Каждый сегмент: соответствует одной категории инициативы
 * - Метки: белые кружки с первой буквой названия категории
 * - Размер: фиксированный outerRadius=58 для обеих диаграмм
 *
 * **Валидация данных:**
 * 1. Проверка наличия входных данных (initiative)
 * 2. Проверка числовых значений на диапазон 0-100%
 * 3. При невалидных значениях пара скрывается (устанавливаются в 0)
 * 4. Если все данные невалидны - компонент не рендерится
 *
 * @param {TTwoPieCharts} props - Свойства компонента
 * @returns {JSX.Element | null} Парные круговые диаграммы или null при некорректных данных
 *
 * @example
 * // Базовое использование с валидными данными
 * const initiativeData = [
 *   { name: 'Коммуникация', prevValue: 30, currentValue: 45 },
 *   { name: 'Социализация', prevValue: 40, currentValue: 55 },
 *   { name: 'Активность', prevValue: 20, currentValue: 35 }
 * ];
 *
 * <TwoPieCharts initiative={initiativeData} />
 *
 * @example
 * // Данные с автоматической фильтрацией невалидных значений
 * const mixedInitiative = [
 *   { name: 'Категория A', prevValue: 30, currentValue: 45 }, // валидно
 *   { name: 'Категория B', prevValue: -10, currentValue: 150 }, // станет 0,0
 *   { name: 'Категория C', prevValue: 60, currentValue: 75 } // валидно
 * ];
 *
 * <TwoPieCharts initiative={mixedInitiative} />
 *
 * @example
 * // Интеграция с системой оценки инициативы
 * const InitiativeDashboard = ({ assessmentData }) => {
 *   const formattedData = assessmentData.dimensions.map(dim => ({
 *     name: dim.category,
 *     prevValue: dim.baselineScore,
 *     currentValue: dim.currentScore
 *   }));
 *
 *   return (
 *     <div className="initiative-section">
 *       <h3>Динамика инициативы по категориям</h3>
 *       <TwoPieCharts initiative={formattedData} />
 *       <Legend categories={assessmentData.dimensions.map(d => d.category)} />
 *     </div>
 *   );
 * };
 *
 * @note
 * - Обе диаграммы имеют одинаковый размер и положение (outerRadius=58, cx/cy="50%")
 * - Кастомные метки используют первую букву названия категории
 * - Белые кружки для меток имеют фиксированный радиус 10px
 * - Метки расположены на расстоянии 5px от внешнего края сегмента
 * - Все значения должны быть в диапазоне 0-100% (сумма может быть ≠ 100)
 * - При невалидных значениях сегменты скрываются (устанавливаются в 0)
 * - Компонент использует CSS-переменные для цветов и фона
 *
 * @warning
 * 1. Сумма значений в каждой диаграмме может не равняться 100%, что может искажать восприятие
 * 2. Первая буква названия категории может быть недостаточно информативной
 * 3. При скрытии сегментов из-за невалидных данных диаграмма может стать пустой
 * 4. Отсутствует легенда, объясняющая соответствие букв и категорий
 * 5. Компонент не адаптивен к разному количеству категорий (фиксированный outerRadius)
 * 6. Белые кружки меток могут перекрываться при большом количестве сегментов
 * 7. CSS-переменные должны быть корректно определены в глобальных стилях
 *
 * @accessibility
 * - Диаграммы являются чисто визуальными элементами без семантической структуры
 * - Кастомные метки могут быть недоступны для скринридеров
 * - Отсутствуют текстовые альтернативы для числовых данных
 * - Рекомендуется добавить role="img" и aria-label для каждой диаграммы
 * - Для скринридеров нужно добавить таблицу с числовыми значениями
 * - Слепые пользователи не смогут получить информацию о соотношении сегментов
 *
 * @data_processing
 * **Обработка входных данных:**
 * ```javascript
 * // Фильтрация невалидных значений (вне диапазона 0-100)
 * if (value >= 0 && value <= 100) {
 *   return значение;
 * } else {
 *   return 0; // сегмент скрывается
 * }
 * ```
 *
 * **Формат данных после обработки:**
 * - name: оригинальное название категории
 * - prevValue: валидированное значение предыдущего периода (0-100 или 0)
 * - currentValue: валидированное значение текущего периода (0-100 или 0)
 *
 * @custom_label
 * **Логика позиционирования меток:**
 * 1. Рассчитывается положение на окружности с учетом угла сегмента
 * 2. Добавляется отступ 5px от внешнего края сегмента
 * 3. Рисуется белый кружок радиусом 10px
 * 4. В центре кружка отображается первая буква названия категории
 *
 * **Математика позиционирования:**
 * ```
 * x = cx + (outerRadius - 10 - 5) * cos(-angle)
 * y = cy + (outerRadius - 10 - 5) * sin(-angle)
 * ```
 * где: 10 - радиус кружка, 5 - отступ от края
 *
 * @layout
 * Структура компонента:
 * ```
 * <div class="container">
 *   <ResponsiveContainer> <!-- Диаграмма предыдущего периода -->
 *     <PieChart>
 *       <Pie data={pieChartData} dataKey="prevValue" ... />
 *     </PieChart>
 *   </ResponsiveContainer>
 *
 *   <ResponsiveContainer> <!-- Диаграмма текущего периода -->
 *     <PieChart>
 *       <Pie data={pieChartData} dataKey="currentValue" ... />
 *     </PieChart>
 *   </ResponsiveContainer>
 * </div>
 * ```
 *
 * **CSS-классы:**
 * - .container - Основной контейнер для двух диаграмм
 * - .pie_prev - Стили для диаграммы предыдущего периода
 * - .pie_current - Стили для диаграммы текущего периода
 *
 * @dependencies
 * - Recharts: библиотека для построения графиков (Pie, PieChart, ResponsiveContainer)
 * - TTwoPieCharts: тип входных данных из внешнего модуля
 *
 * @see Recharts - Библиотека для построения графиков
 * @see PieChart.module.css - Стили компонента
 *
 * @todo
 * - Добавить легенду с полными названиями категорий и цветовой кодировкой
 * - Реализовать адаптивный outerRadius в зависимости от количества категорий
 * - Добавить tooltip с детальной информацией по каждому сегменту
 * - Реализовать анимацию изменений между периодами
 * - Добавить валидацию суммы значений (опционально требовать 100%)
 * - Реализовать режим отображения процентов внутри сегментов
 * - Поддержка темной/светлой темы через CSS-переменные
 * - Добавить возможность выбора типа меток (буква/процент/иконка)
 * - Реализовать единую диаграмму с двумя кольцами вместо двух отдельных
 * - Добавить интерактивность (клик по сегменту для деталей)
 * - Поддержка локализации названий категорий
 * - Реализовать сравнение с нормативными значениями
 * - Добавить экспорт диаграмм как изображения
 * - Реализовать кастомные цвета для разных категорий
 * - Добавить заголовки для каждой диаграммы ("До", "После")
 * - Реализовать обработку случаев с большим количеством категорий
 *
 * @design
 * Компонент решает следующие задачи:
 * 1. Визуальное сравнение распределения показателей по категориям между двумя периодами
 * 2. Интуитивное представление изменений в структуре инициативы
 * 3. Создание наглядного инструмента для анализа многомерных данных
 * 4. Интеграция с системами оценки и мониторинга развития
 * 5. Предоставление быстрого обзора динамики без детального изучения чисел
 *
 * @visual_perception
 * **Сильные стороны дизайна:**
 * - Парное расположение облегчает прямое сравнение
 * - Одинаковый размер обеспечивает честное сравнение
 * - Белые метки с буквами улучшают читаемость
 * - Простая цветовая схема (два набора цветов) уменьшает когнитивную нагрузку
 *
 * **Ограничения восприятия:**
 * - Человеческий глаз плохо оценивает относительные размеры сегментов
 * - Отсутствие числовых значений затрудняет точное сравнение
 * - Цвета могут быть неразличимы для дальтоников
 * - При малых различиях изменения могут быть незаметны
 *
 * @best_practices
 * **Рекомендации по использованию:**
 * 1. Использовать не более 5-7 категорий для сохранения читаемости
 * 2. Обеспечить достаточный контраст между соседними сегментами
 * 3. Дополнять диаграммы таблицей с точными числовыми значениями
 * 4. Использовать в сочетании с другими визуализациями для полной картины
 * 5. Предоставлять текстовую интерпретацию наблюдаемых изменений
 *
 * **Альтернативные визуализации:**
 * - Столбчатая диаграмма для точного сравнения значений
 * - Радарная диаграмма для многомерного сравнения
 * - Пузырьковая диаграмма для отображения трех параметров
 * - Тепловая карта для выявления паттернов изменений
 */

export const TwoPieCharts: React.FC<TTwoPieCharts> = ({ initiative }) => {
  if (!initiative) return null;

  // проверка входящих данных: если хотя бы одно из значений не является числом в диапазоне от 0 до 100 включительно,
  // то вся пара (и предыдущее значени, и текущее) не отображаются на графике
  const pieChartData: TPieChartData[] = initiative.map(item => {
    const numPrevValue = Number(item.prevValue);
    const numCurrentValue = Number(item.currentValue);
    if (
      numCurrentValue >= 0 &&
      numCurrentValue <= 100 &&
      numPrevValue >= 0 &&
      numPrevValue <= 100
    ) {
      return {
        ...item,
        prevValue: numPrevValue,
        currentValue: numCurrentValue,
      };
    } else {
      return { ...item, prevValue: 0, currentValue: 0 };
    }
  });

  if (!pieChartData) return null;

  const CustomLabel = ({
    cx,
    cy,
    midAngle,
    outerRadius,
    index,
  }: PieLabelRenderProps) => {
    const RADIAN = Math.PI / 180;
    // отступ от внешнего края сегмента
    const offset = 5;
    // радиус белого кружка
    const circleRadius = 10;
    const radius = Number(outerRadius) - circleRadius - offset;
    const ncx = Number(cx);
    const x = ncx + radius * Math.cos(-(midAngle ?? 0) * RADIAN);
    const ncy = Number(cy);
    const y = ncy + radius * Math.sin(-(midAngle ?? 0) * RADIAN);
    // формат подписи данных - первая буква названия категории
    const firstLetter = initiative[index].name.charAt(0);
    return (
      <g>
        <circle
          cx={x}
          cy={y}
          r={circleRadius}
          fill="var(--background-main-color, #fff)"
        />
        <text
          x={x}
          y={y}
          fill="var(--text-main-color, #141414)"
          textAnchor="middle"
          dominantBaseline="central"
          style={{ fontSize: '16px', fontWeight: '700' }}
        >
          {firstLetter}
        </text>
      </g>
    );
  };

  return (
    <div className={styles.container}>
      <ResponsiveContainer width="100%" height="100%">
        <PieChart>
          <Pie
            data={pieChartData}
            dataKey="prevValue"
            className={styles.pie_prev}
            cx="50%"
            cy="50%"
            labelLine={false}
            label={CustomLabel}
            outerRadius={58}
            isAnimationActive={false}
          />
        </PieChart>
      </ResponsiveContainer>
      <ResponsiveContainer width="100%" height="100%">
        <PieChart>
          <Pie
            data={pieChartData}
            dataKey="currentValue"
            className={styles.pie_current}
            cx="50%"
            cy="50%"
            labelLine={false}
            label={CustomLabel}
            outerRadius={58}
            isAnimationActive={false}
          />
        </PieChart>
      </ResponsiveContainer>
    </div>
  );
};
